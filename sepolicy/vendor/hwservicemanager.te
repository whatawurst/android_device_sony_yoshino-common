# hwservicemanager.te

allow hwservicemanager thermal-engine:dir search;
allow hwservicemanager thermal-engine:file { open read } ;
allow hwservicemanager thermal-engine:process getattr;

allow hwservicemanager updatemiscta:dir r_dir_perms;

allow hwservicemanager modem_switcher:binder { call transfer };
allow hwservicemanager modem_switcher:dir search;
allow hwservicemanager modem_switcher:file { open read } ;
allow hwservicemanager modem_switcher:process getattr;
